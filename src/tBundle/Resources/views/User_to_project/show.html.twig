{% extends '::base.html.twig' %}

{% block body -%}
    <h1>User_to_project</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
            <tr>
                <th>Statecreator</th>
                <td>{{ entity.stateCreator }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('user_to_project') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('user_to_project_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
